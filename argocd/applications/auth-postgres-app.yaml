apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auth-postgres
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/PlainMarket/gitops-infra.git
    targetRevision: main
    path: auth-postgres/helm
    helm:
      releaseName: auth-postgres
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true