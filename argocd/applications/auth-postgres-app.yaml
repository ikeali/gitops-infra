apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auth-postgres
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ikeali/gitops-infra.git
    targetRevision: main
    path: helm/auth-postgres
    helm:
      releaseName: auth-postgres
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true