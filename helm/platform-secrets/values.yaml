vault:
  # server: https://vault.edusuc.net
  server: https://vault-playground.edusuc.net
  path: Dev-secret
  auth:
    userPass:
      path: userpass
      username: s12ike.wft
      credentialsSecretName: vault-auth
      credentialsSecretKey: password
