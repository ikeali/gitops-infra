replicaCount: 2

image:
  repository: ghcr.io/plainmarket/order-service
  tag: "ed86eaca32a2cf6b8e732171cc89a33b8cb5d208"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

containerPort: 8000

tolerations:
  - operator: Exists

env: {}

database:
  host: order-postgres
  port: "5432"

secrets:
  jwtSecretName: jwt-secret
  orderDbSecretName: order-db-secret
