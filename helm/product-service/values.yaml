replicaCount: 2

image:
  repository: ghcr.io/plainmarket/product-service 
  tag: "db013aadaa3246b9ff7a4ddc98c5f3ba60e54292"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

containerPort: 8000

tolerations:
  - operator: Exists

env:
  USER_SERVICE_URL: "http://user-service:8002"

database:
  host: product-postgres
  port: "5432"

secrets:
  jwtSecretName: jwt-secret
  productDbSecretName: product-db-secret