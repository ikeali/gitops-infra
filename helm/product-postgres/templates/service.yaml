apiVersion: v1
kind: Service

metadata:
  name: product-postgres

spec:
  type: {{ .Values.service.type }}

  selector:
    app: product-postgres

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432